#!/usr/bin/env node
/**
 * DEPRECATED: Contracts are now centralized in packages/@ia-next/contracts.
 * This script is kept as a no-op to avoid breaking older CI pipelines 
 * that still call it during transition.
 */
console.log("✅ Contracts are centralized in @ia-next/contracts. Sync is no longer required.");
process.exit(0);
