export const metadata = {
  title: "Visualizador de Notas",
  description: "Visualiza y analiza calificaciones",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
