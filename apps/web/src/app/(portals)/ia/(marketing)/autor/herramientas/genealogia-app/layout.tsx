export const metadata = {
  title: "Genealogía App",
  description: "Herramienta de genealogía interactiva",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
