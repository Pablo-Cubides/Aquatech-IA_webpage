# Matrices Generator (EIA)

Route: `/ambiental/herramientas/generador-matrices`

Purpose
- Guided generator for EIA matrices (Leopold, Conesa, Battelle). Produces printable matrices for impact assessment.

Developer notes
- Core logic lives in `packages/@ia-next/matriz-generator`. Keep package interface stable for reuse.
